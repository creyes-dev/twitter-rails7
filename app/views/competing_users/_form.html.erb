<%= form_with(model: competing_user) do |form| %>
  <% if competing_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(competing_user.errors.count, "error") %> prohibited this competing_user from being saved:</h2>

      <ul>
        <% competing_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :competition_id, style: "display: block" %>
    <%= form.text_field :competition_id %>
  </div>

  <div>
    <%= form.label :competing_team_id, style: "display: block" %>
    <%= form.text_field :competing_team_id %>
  </div>

  <div>
    <%= form.label :score, style: "display: block" %>
    <%= form.number_field :score %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
