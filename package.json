{
  "name": "myapp",
  "private": "true",
  "scripts": {
    "build": "esbuild app/javascript/*.* --bundle --outdir=app/assets/builds"
  },
  "dependencies": {
    "@hotwired/turbo-rails": "^7.1.3",
    "esbuild": "^0.15.7",
    "stimulus": "^3.1.0"
  }
}
